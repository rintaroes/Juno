EXPO_PUBLIC_SUPABASE_URL=https://glxrlijpjskuskgfxoif.supabase.co/
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_3qHccvSko3aqcFnF9qYA6w_pxc7F2cV

# Android Map tab: Google Maps SDK key (package + SHA restricted). Optional on iOS (MapKit).
# EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_API_KEY=

# Expo push (expo-notifications): UUID from `npx eas-cli init` / project dashboard → merged in app.config.ts as extra.eas.projectId.
# EXPO_PUBLIC_EAS_PROJECT_ID=

# Supabase Edge Function secrets (Dashboard → Edge Functions → Secrets), not Expo env:
# EXPO_ACCESS_TOKEN — optional Bearer for https://exp.host/--/api/v2/push/send if your Expo project requires it.